# E2E test dependencies
# Install with: pip install -r requirements-test.txt
# Then: playwright install chromium

pytest>=8.3.5
pytest-asyncio>=0.24.0
playwright>=1.50.0
httpx>=0.28.1

# greenlet is a transitive dep of playwright on Windows; needs >=3.1.1 for Python 3.14
greenlet>=3.1.1

# For direct DB access during reset helpers
asyncpg>=0.30.0

# Test data generation
faker>=33.1.0

# TOTP code generation for MFA enrollment tests
pyotp>=2.9.0

# Required by aioboto3/botocore for S3 request signing (test_17)
cryptography>=48.0.1
aioboto3>=13.3.0

# In-process Redis emulator for unit tests (test_storage_providers.py)
fakeredis>=2.26.0

# Azure SDK for Azurite E2E tests (test_32)
azure-storage-blob[aio]>=12.19.0
